langcode: en
status: true
dependencies:
  enforced:
    module:
      - farm_bee
      - farm_migrate
id: farm_migrate_asset_bee_colony
label: 'Assets (Bee colony)'
migration_group: farm_migrate_asset
migration_tags:
  - 'Drupal 7'
  - 'farmOS 1.x'
class: Drupal\migrate\Plugin\Migration
field_plugin_method: null
cck_plugin_method: null
source:
  plugin: d7_asset
  bundle: bee_colony
destination:
  plugin: 'entity:asset'
process:
  # Hard-code the bundle.
  type:
    plugin: default_value
    default_value: bee_colony
  # Maintain the asset ID.
  id:
    plugin: get
    source: id
migration_dependencies:
  required: {  }
  optional: {  }
